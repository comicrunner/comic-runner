{
  "name": "comic-runner", // Project name (unique identifier)
  "type": "module", // Uses ES modules for modern JavaScript
  "version": "0.0.1", // Initial version (semantic versioning)
  "scripts": {
    // Predefined npm commands for project
    "dev": "astro dev", // Starts development server
    "build": "astro build" // Builds production site
  },
  "dependencies": {
    // Core project dependency
    "astro": "^4.5.3" // Latest Astro version (^ allows minor updates)
  }
}
